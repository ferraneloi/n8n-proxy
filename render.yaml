services:
  - type: web
    name: n8n-proxy
    env: node
    plan: free
    branch: main
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: TARGET_URL
        value: "http://localhost:5678"